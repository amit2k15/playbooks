---
 - hosts: all
   connection: local
   gather_facts: false

   tasks:
   - name: Print nested json input
     debug:
        msg: "impactedEntities.name : {{ impactedEntities}}"
   tasks:
   - name: restart zabbixagent
     service:
       name: "{{ name }}"
       state: restarted     
