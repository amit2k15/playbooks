---
 - hosts: all
   connection: local
   gather_facts: false

   tasks:
   - name: Print nested json input
     debug:
        msg: "impactedEntities : {{ impactedEntities }}"
   - name: Restart the service
     service:
        name: "{{ ProblemDetailsHTML }}"
        state: restarted
