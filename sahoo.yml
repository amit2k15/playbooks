---
 - hosts: all
   connection: local
   gather_facts: false

   tasks:
   - name: Print nested json input
     debug:
        msg: "impactedEntities : {{ impactedEntities }} || impactedEntities.type : {{ impactedEntities.type[0] }} || impactedEntities.name : {{  impactedEntities.name[0] }}"
