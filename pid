#!/bin/bash

# Define the discovery data in JSON format
echo '{"data":['

# Iterate through each process and extract relevant information
ps aux --sort=-%cpu | awk 'NR>1 {print "{\"{#PROCESS_NAME}\":\"" $11 "\",\"{#PID}\":\"" $2 "\"},"}'

echo ']}'
