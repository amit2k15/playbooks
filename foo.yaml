---
 - hosts: all
   connection: local
   gather_facts: false

   tasks:
   - name: Print nested json input
     debug:
        msg: "impactedEntities : {{ repo }} || impactedEntities.branch : {{ repo.branch }}"
